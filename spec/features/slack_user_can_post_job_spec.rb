require 'rails_helper'

describe "Slack user can post their job" do
  scenario "they type /jobs and can see two options" do
    # As a Turing Slack User
    # When I type 'jobs' into Slack
    # I see a list of options for Stats, and if I have graduated, I also see Add a Job, and My Jobs
  end
end